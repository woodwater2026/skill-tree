# Tools

This directory contains ingestion, normalization, and audit tooling for Skill Tree.

- `ingest/` — pull skills from curated sources
- `normalize/` — clean and map metadata to schema
- `audit/` — generate static security findings and reports
